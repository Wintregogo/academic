# 核心依赖
arxiv>=2.1.0
pdfplumber>=0.11.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0          # 必须！用于 BeautifulSoup 的 XML 解析

# Web 与缓存
streamlit>=1.30.0
python-dotenv>=1.0.0 # 可选：用于管理 API 密钥（如 .env）

# LLM 支持（按需安装，保留常用后端）
openai>=1.10.0       # OpenAI API
dashscope>=1.14.0    # 阿里云 Qwen（通义千问）
ollama>=0.1.0        # 本地 Ollama 模型（可选）

# 工具库
tqdm>=4.66.0         # 进度条（可选，用于批量处理）
pathlib>=1.0.1       # 路径操作（Python 3.4+ 内置，但显式列出更安全）